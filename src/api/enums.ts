export enum Version {
  V1 = "V1",
  V2 = "V2",
}

export enum EthereumNetwork {
  kovan = "kovan",
  ropsten = "ropsten",
  main = "main",
}

export enum AavePool {
  proto = "proto",
  uniswap = "uniswap",
}
